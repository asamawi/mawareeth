# Retired: GitHub Actions is the only CI and release producer for this project.
# This file remains temporarily for external configuration discovery; it defines
# no build or deployment behavior.
if: false
